version: 2

jobs:
  print_version:
    docker:
      - image: buildpack-deps:bionic-curl
    steps:
      - heroku/install
      - run: heroku --version

workflows:
  version: 2
  dev:
    jobs:
      - print_version